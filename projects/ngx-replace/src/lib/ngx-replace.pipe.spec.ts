import { NgxReplacePipe } from './ngx-replace.pipe';

describe('NgxReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new NgxReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
